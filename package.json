{
  "name": "wealthplanner",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "check": "turbo run typecheck test lint",
    "engine:version-guard": "node scripts/engine-version-guard.mjs",
    "style-guard": "node scripts/style-guard.mjs",
    "sources:check": "node scripts/check-sources.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "fast-check": "^4.1.1",
    "prettier": "^3.6.0",
    "turbo": "^2.5.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
